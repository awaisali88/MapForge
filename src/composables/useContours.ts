import type { Map as MaplibreMap } from "maplibre-gl";
import type { ShallowRef } from "vue";

/** DEM contour lines via maplibre-contour. Stub — replaced in Phase 4. */

export function useContours(_map: ShallowRef<MaplibreMap | null>): void {}
