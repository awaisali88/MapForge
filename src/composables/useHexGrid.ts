import type { Map as MaplibreMap } from "maplibre-gl";
import type { ShallowRef } from "vue";

/** H3 hexagon grid overlay. Stub — replaced in Phase 5. */

export function useHexGrid(_map: ShallowRef<MaplibreMap | null>): void {}
