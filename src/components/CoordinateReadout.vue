<script setup lang="ts">
/** Bottom-right cursor coordinate overlay. Stub — replaced in Phase 2. */
defineProps<{ text: string }>();
</script>

<template>
  <span v-if="false">{{ text }}</span>
</template>
